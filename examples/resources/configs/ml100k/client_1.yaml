client_id: "Client1"

train_configs:
  # Device
  device: "cpu"
  # Logging and outputs
  logging_output_dirname: "./output"
  logging_output_filename: "result"

# Local dataset
data_configs:
  dataset_path: "./resources/dataset/ml100k_dataset.py"
  dataset_name: "get_ml100k"
  dataset_kwargs:
    num_clients: 2
    client_id: 0
    partition_strategy: "iid"
    visualization: True
    output_dirname: "./output"
    output_filename: "visualization.pdf"

comm_configs:
  grpc_configs:
    server_uri: localhost:50051
    max_message_size: 1048576
    use_ssl: False
